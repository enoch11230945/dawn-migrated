{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .emotional-story-section {
      background-color: {{ section.settings.bg_color }};
      overflow: hidden;
  }

  .story-layout {
      align-items: center;
  }

  .story-image-wrapper {
    position: relative;
    border-radius: var(--media-radius);
    overflow: hidden;
    box-shadow: var(--media-shadow-horizontal-offset) var(--media-shadow-vertical-offset) var(--media-shadow-blur-radius) rgba(var(--color-shadow), var(--media-shadow-opacity));
  }

  .story-image-wrapper::after {
    content: '';
    position: absolute;
    bottom: -2rem;
    right: -2rem;
    width: 20rem;
    height: 20rem;
    background: linear-gradient(135deg, rgba(212,165,116,0.2), rgba(196,148,100,0.1));
    border-radius: 50%;
    z-index: 1;
    pointer-events: none;
  }

  .story-image {
    width: 100%;
    height: auto;
    display: block;
    object-fit: cover;
  }

  .story-content-col {
      padding-left: 2rem;
  }

  .story-eyebrow {
    display: inline-block;
    color: #d4a574;
    font-size: 1.2rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
  }

  .story-heading {
    margin-bottom: 2rem;
    color: rgb(var(--color-foreground));
  }

  .story-body {
    font-size: 1.6rem;
    color: rgba(var(--color-foreground), 0.75);
    line-height: 1.8;
    margin-bottom: 3rem;
  }

  .story-body p {
    margin-bottom: 1.5rem;
  }

  .story-body p:last-child {
    margin-bottom: 0;
  }

  .story-quote {
    background: rgba(var(--color-foreground), 0.03);
    padding: 2.5rem 3rem;
    border-left: 4px solid #d4a574;
    margin: 3rem 0;
    border-radius: 0 var(--media-radius) var(--media-radius) 0;
  }

  .story-quote p {
    font-size: 1.8rem;
    font-style: italic;
    color: rgb(var(--color-foreground));
    line-height: 1.6;
    margin: 0 0 1rem;
    font-family: var(--font-heading-family);
  }

  .story-quote cite {
    font-size: 1.3rem;
    color: rgba(var(--color-foreground), 0.6);
    font-style: normal;
    display: block;
    margin-top: 1rem;
  }

  .story-cta {
     margin-top: 2rem;
  }

  @media screen and (max-width: 750px) {
      .story-content-col {
          padding-left: 0;
          margin-top: 3rem;
          text-align: center;
      }
      .story-quote {
          text-align: left;
      }
      .story-image-wrapper::after {
          display: none;
      }
  }

{%- endstyle -%}

<div class="emotional-story-section section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="grid grid--1-col grid--2-col-desktop story-layout">
      
      <div class="grid__item story-image-col">
        {%- if section.settings.image != blank -%}
          <div class="story-image-wrapper">
             {{ section.settings.image | image_url: width: 800 | image_tag: 
                class: 'story-image', 
                loading: 'lazy', 
                sizes: '(min-width: 990px) 50vw, 100vw',
                widths: '375, 550, 750, 1100, 1500'
             }}
          </div>
        {%- else -%}
             <div class="story-image-wrapper">
               {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
             </div>
        {%- endif -%}
      </div>
      
      <div class="grid__item story-content-col">
        {%- if section.settings.eyebrow != blank -%}
          <span class="story-eyebrow">{{ section.settings.eyebrow | escape }}</span>
        {%- endif -%}
        
        <h2 class="story-heading {{ section.settings.heading_size }}">{{ section.settings.heading | escape }}</h2>
        
        <div class="story-body">
          {{ section.settings.content }}
        </div>
        
        {%- if section.settings.quote != blank -%}
          <blockquote class="story-quote">
            <p>"{{ section.settings.quote | escape }}"</p>
            {%- if section.settings.quote_author != blank -%}
              <cite>— {{ section.settings.quote_author | escape }}</cite>
            {%- endif -%}
          </blockquote>
        {%- endif -%}
        
        {%- if section.settings.button_text != blank -%}
          <a href="{{ section.settings.button_link }}" class="button button--primary story-cta">
            {{ section.settings.button_text | escape }}
          </a>
        {%- endif -%}
      </div>
      
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Emotional Story",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow text",
      "default": "Our Story"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Because Saying 'I Love You' Shouldn't Be Hard"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        { "value": "h2", "label": "Small" },
        { "value": "h1", "label": "Medium" },
        { "value": "h0", "label": "Large" },
        { "value": "hxl", "label": "Extra Large" }
      ],
      "default": "h1",
      "label": "Heading Size"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content",
      "default": "<p>We get it. You love them. But when it comes to saying it out loud, the words get stuck somewhere between your heart and your mouth.</p><p>That's where we come in. Our necklaces come paired with beautifully written message cards that say exactly what you're feeling—so you don't have to find the words yourself.</p><p>It's not about the jewellery. It's about the moment they read that card and feel how much they mean to you.</p>"
    },
    {
      "type": "textarea",
      "id": "quote",
      "label": "Quote",
      "default": "She cried when she read the card. That's when I knew I'd got it right."
    },
    {
      "type": "text",
      "id": "quote_author",
      "label": "Quote author",
      "default": "James, Manchester"
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "View Our Collection"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 36
    }
  ],
  "presets": [
    {
      "name": "Emotional Story"
    }
  ]
}
{% endschema %}
